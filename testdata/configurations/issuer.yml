# Assumes 1) $PWD = irmago and 2) irmamobile exists next to irmago. Overwrite e.g. with -s if necessary
schemes_path: ../irmamobile/irma_configuration

schemes_update: 0
revocation_settings:
  irma-demo.MijnOverheid.root:
    revocation_server_url: http://localhost:8090/irma/
    sse: true